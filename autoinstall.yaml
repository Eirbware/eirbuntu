autoinstall:
  version: 1
  name: "Eirbuntu"
  
  interactive-sections:
    - storage
    - networks
    - locale
    - keyboard
    - identity
  
  packages:
    - git
    - vim 
    - emacs
    - neovim
    - valgrind
    - gdb

  late-commands:
    - snap install codium --classic

  early-commands:
    - /cdrom/commands-before-install.sh 2> /error.log
